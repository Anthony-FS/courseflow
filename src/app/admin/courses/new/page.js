import { AddCourseForm } from "@/components/add-course-form";

export const metadata = {
  title: "Add Course | Admin | CourseFlow",
};

export default function AddCoursePage() {
  return <AddCourseForm />;
}
